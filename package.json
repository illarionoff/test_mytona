{
  "name": "mytona-test",
  "version": "1.0.0",
  "description": "MyTona Test Assignment",
  "main": "server.js",
  "scripts": {
    "client-install": "npm install --prefix client",
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "nodemon server.js",
    "client": "npm start --prefix client",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "author": "Illarionov Afanasiy",
  "license": "MIT",
  "dependencies": {
    "concurrently": "^4.1.0",
    "json-server": "^0.14.2",
    "multer": "^1.4.1"
  },
  "devDependencies": {
    "nodemon": "^1.18.10"
  }
}
